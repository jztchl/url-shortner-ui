import axios from "axios";

const API = "http://127.0.0.1:9000"; 

export const shortenUrl = async (original_url) => {
   
  const res = await axios.post(`${API}/shorten`, { original_url });
  return res.data;
};
